# CLAUDE.md — инструкция для ИИ-агентов

Читается в начале каждой сессии. Устаревший файл хуже отсутствующего: если правило
здесь разошлось с кодом — сначала чиним файл, потом пишем код.

## 1. Что это за проект

**Aerogram Logistic OS** — мультитенантная платформа между грузоотправителем и
транспортными компаниями: расчёт → выбор → создание → трекинг → документы → аналитика.

Текущий релиз — **MVP-0**: один перевозчик (СДЭК), один боевой тенант («Роспломба»),
режим `own_contract` (обращение к API перевозчика под учётными данными клиента).

ТЗ: `docs/tz/` (функциональная часть) и `docs/tz/` (реализация). Решения — `docs/adr/`.

## 2. Стек (менять только через ADR)

| Слой | Версия |
|---|---|
| Python | 3.12 |
| FastAPI | 0.115+ |
| SQLAlchemy | 2.0, async, императивный стиль |
| Pydantic | v2 + pydantic-settings |
| Alembic | миграции |
| PostgreSQL | 16 (RLS обязательна) |
| Redis | 7 (кэш, идемпотентность, брокер) |
| Celery | 5 + Redis |
| httpx + tenacity | внешние HTTP-вызовы |
| structlog | JSON-логи |
| React | 18 + TypeScript 5 + Vite |
| Ant Design | 5 |
| TanStack Query | 5 |
| Zustand | локальное состояние |

**Новая зависимость — решение человека, с записью в ADR.** Агент не добавляет
библиотеки самостоятельно.

## 3. Карта модулей

```
backend/src/aerogram/
├── main.py         сборка FastAPI
├── config.py       Pydantic Settings, только переменные окружения
├── db.py           движок, сессии, установка app.tenant_id
├── shared/         типы, ошибки, деньги, время, календарь, uuid7, крипто
├── core/           тенанты, пользователи, права, API-ключи, аудит
├── directories/    ФИАС/ДаData, города, city_carrier_map, терминалы
├── carriers/       адаптеры ТК
│   ├── base.py     протокол CarrierAdapter + DTO — контракт, меняется через ADR
│   ├── registry.py реестр адаптеров
│   ├── status_map/ YAML-карты статусов ТК → нормализованные
│   └── cdek/       адаптер СДЭК
├── rating/         rate shopping, кэш, ранжирование
├── shipments/      создание, отмена, идемпотентность, сверка «призраков»
├── tracking/       вебхуки, polling, лента событий
├── documents/      этикетки, реестры, опись, S3
├── scoring/        Carrier Score
├── reports/        план/факт, выгрузки
├── notifications/  почта, Telegram
└── worker/         задачи Celery, расписания
```

Внутри модуля: `models.py` (SQLAlchemy), `schemas.py` (Pydantic DTO),
`repository.py` (единственное место с SQL), `service.py` (логика), `router.py` (HTTP).

## 4. Правила зависимостей (проверяются import-linter в CI)

Слои внутри модуля, сверху вниз — зависимости только вниз:

```
router  →  service  →  repository  →  models
                ↓
             shared
```

Межмодульные правила:

1. `carriers` **не знает** ни о `shipments`, ни о `rating`, ни о `tracking`.
   Адаптер принимает DTO и возвращает DTO.
2. `rating`, `shipments`, `tracking` обращаются к перевозчикам **только** через
   `carriers.registry`, никогда напрямую к `carriers.cdek` и т. п.
3. `scoring` и `reports` работают **только на чтение** и не вызывают адаптеры.
4. `shared` не зависит ни от чего внутри проекта.
5. Любой доступ к БД — через `repository.py` модуля. SQL вне репозиториев запрещён.

Контракты: `.importlinter`. Красный import-linter — блокирующая ошибка CI.

## 5. Команды

```bash
make up             # поднять локальное окружение (postgres, redis, mailpit, minio)
make down
make migrate        # alembic upgrade head
make revision m="…" # новая миграция (автогенерация ТОЛЬКО как черновик)
make api            # uvicorn с автоперезагрузкой
make worker         # celery worker + beat
make lint           # ruff + mypy + import-linter
make test           # pytest
make front          # vite dev server
make openapi        # выгрузить openapi.json и перегенерировать клиент фронта
```

## 6. Соглашения по коду

**Типизация.** `mypy --strict` для `shared/`, `carriers/`, `rating/`, `shipments/`.
`Any` не появляется в диффе. Публичные функции аннотированы полностью.

**Деньги.** Только `Decimal`, в БД `NUMERIC(12,2)`. `float` для денег — ошибка ревью.
Округление — `shared.money.round_money` (ROUND_HALF_UP, 2 знака).

**Время.** Только timezone-aware UTC (`shared.clock.utcnow`), в БД `TIMESTAMPTZ`.
Отображение — по таймзоне тенанта, на фронте. `datetime.now()` без tz запрещён.

**Идентификаторы.** Первичные ключи — UUIDv7 (`shared.ids.uuid7`), сортируемы по времени.

**Ошибки.** Иерархия `shared.errors.AerogramError`. Ошибка перевозчика **никогда** не
даёт `500` — она превращается в строку выдачи с кодом и русским текстом.
Формат ответа API единый: `{"error": {"code", "message", "field", "carrier_code", "request_id"}}`.

**Внешние вызовы.** Всегда таймаут (connect + read), ретраи через tenacity,
обработка ошибки. httpx-клиент без явного таймаута — ошибка ревью.

**Тенанты.** Каждая бизнес-таблица содержит `tenant_id` и политику RLS.
Транзакция начинается с `SET LOCAL app.tenant_id`. Приложение подключается к БД
ролью **без** `BYPASSRLS`. Доступ к чужому объекту по прямому идентификатору
даёт `404`, а не `403`.

**Персональные данные.** ФИО, телефоны, адреса не попадают в логи и трассировки —
маскирование через `shared.logging.mask_pd`. Сырые ответы ТК хранятся 30 суток.

**Язык.** Интерфейс, письма, тексты ошибок — русский. Код, имена, коммиты — английский.

## 7. Что нельзя делать без построчного ревью человека

Здесь ошибка стоит дороже всего и обнаруживается позже всего:

1. Схема БД и миграции.
2. RLS и всё, что касается изоляции тенантов.
3. Контракт `CarrierAdapter` (`carriers/base.py`).
4. Идемпотентность и сверка «призраков».
5. Работа с деньгами: округления, типы, валюта.
6. Аутентификация, права, хранение секретов.
7. Обработка персональных данных.

По каждому пункту решение принимает человек и фиксирует в ADR; агент реализует уже
принятое решение.

## 8. Гигиена

- Агент не «улучшает» код вне поставленной задачи. Любая правка за пределами задачи —
  отдельный PR.
- Агент **не правит фикстуры и не ослабляет ассерты**, чтобы тест позеленел.
  Красный тест — сигнал, а не препятствие.
- Агент не меняет миграции, уже применённые в staging или prod. Только новая миграция.
- Тесты пишутся вместе с кодом, в той же задаче. Отложенные тесты не пишутся никогда.
- Ветка на задачу, PR с описанием, зелёный CI до слияния.
- Каждое архитектурное решение — короткий ADR в `docs/adr/`.

## 9. Что должно быть в PR

- [ ] Границы модулей не нарушены (import-linter зелёный)
- [ ] Типы строгие, `Any` не появился
- [ ] Все внешние вызовы имеют таймаут и обработку ошибки
- [ ] Запросы к БД идут через репозиторий и содержат `tenant_id`
- [ ] Деньги — `Decimal`, время — `TIMESTAMPTZ` в UTC
- [ ] Персональные данные не попадают в логи
- [ ] Тесты покрывают успешный путь и минимум один сбойный
- [ ] Миграция обратима (`downgrade` реализован)
- [ ] Обновлены OpenAPI-схема и сгенерированный клиент фронта
- [ ] Дифф прочитан человеком целиком
